# Copy this file to ".env" and fill in your values.
# The AI keys are optional — only needed for the AI fallback on unknown PDF layouts.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# AI provider for unknown-format fallback: openai | claude | both | disabled
AI_PROVIDER=disabled

# SQLite database location
DATABASE_PATH=data/vesseliq.sqlite
